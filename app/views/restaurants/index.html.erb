<ul>
  <% @restaurants.each do |restaurant| %>
    <li> <h2> <%= restaurant.name %> </h2> <%= link_to "en savoir plus", restaurant_path(restaurant), class: "btn btn-primary"%> </li>
  <% end %>
</ul>


<%= link_to 'ajouter un resto', new_restaurant_path, class: "btn btn-primary" %>
